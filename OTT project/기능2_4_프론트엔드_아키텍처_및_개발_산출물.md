# OTT 기능2·4 — 최종 프론트엔드 개발 아키텍처 및 산출물

> **작성일**: 2026-07-03 (최종)
> **이전 버전**: 2026-06-22 → 2026-06-23 → 2026-06-25 → 2026-06-26 → 2026-07-01 → **2026-07-03 (현재)**
> **담당**: 염정운
> **범위**: detailPage(기능2) + myPage(기능4) 프론트엔드 전체

---

## 0. 프로젝트 아키텍처 — 최종

### 0.1 풀스택 _ott 독립

모든 OTT 모듈은 `_ott` 접미사를 사용하며, 원본 템플릿은 **일체 수정하지 않는다**.

```
cap-node/
├── core/          ← 원본 (절대 손 안 댐)
├── ott/           ← OTT 비즈니스 백엔드 (DetailService, MembershipService, MainService)
└── core_ott/      ← OTT 공통 백엔드 (MenuManagement, UserManagement, CodeManagement, MessageManagement)

cap-app/
├── library/       ← 원본 (절대 손 안 댐)
├── library_ott/   ← OTT 공통 UI5 라이브러리 (namespace: common_ott.lib)
├── approuter/     ← 원본
├── approuter_ott/ ← OTT 진입점 + 라우팅
├── portal_ott/    ← OTT 포털
└── sysmgt_ott/    ← OTT 기능 모듈
    └── webapp/
        ├── detailPage/   ← 기능2: 컨텐츠 상세
        └── myPage/       ← 기능4: 마이페이지
```

### 0.2 사용자 요청 흐름 (최종)

```
브라우저 (http://localhost:5000)
  │
  ├─ /sysmgt_ott/*      → approuter_ott (정적 파일 서빙)
  ├─ /common_ott.lib/*  → library_ott/src
  ├─ /srv-api/ott-core/* → onOttCoreService (port 8084)
  └─ /srv-api/*          → onOttService (port 8083)
```

### 0.3 실행 방법

```bash
# 터미널 1: OTT 비즈니스 백엔드
cd cap-node/ott && npx cds watch --port 8083

# 터미널 2: OTT 공통 백엔드
cd cap-node/core_ott && npx cds watch --port 8084

# 터미널 3: 앱라우터
cd cap-app/approuter_ott && npm run start:local
```

| 페이지 | URL | 로그인 |
|--------|-----|--------|
| detailPage | `http://localhost:5000/sysmgt_ott/detailPage/webapp/index.html#/Detail/C001` | U001 / 빈칸 |
| myPage | `http://localhost:5000/sysmgt_ott/myPage/webapp/index.html#/MyMembership` | U001 / 빈칸 |
| PlanChange | `http://localhost:5000/sysmgt_ott/myPage/webapp/index.html#/PlanChange` | U001 / 빈칸 |

### 0.4 아키텍처 의도

| 설계 결정 | 이유 |
|-----------|------|
| **MyMembership ↔ PlanChange 분리** | 조회(Membership)와 액션(PlanChange)의 책임 분리. SAP Fiori List Report → Object Page 패턴과 동일. 하나의 화면에 모든 기능을 넣으면 UI가 과도하게 길어지고 상태 분기 로직이 복잡해짐 |
| **`DynamicPage` 제거, `ObjectPageLayout` 루트 사용** | 두 컨트롤러는 서로 다른 스크롤 컨테이너. 중첩 시 spacer 무한 확장 버그 발생. `ObjectPageLayout`만으로 `snappedContent`·`expandedContent`·`actions`를 네이티브 지원하므로 래퍼 불필요 |
| **`sap.ui.table.Table` → `sap.m.Table`** | 출연진 데이터는 3~5행으로 소규모. `sap.ui.table.Table`의 기본 `visibleRowCount=10`이 빈 행을 생성하고 height 계산을 복잡하게 함. 단순 표는 `sap.m.Table`이 적합 |
| **i18n 완전 분리** | 모든 UI 텍스트를 `i18n.properties`에서 관리. 언어 추가 시 파일 하나만 추가하면 됨 |
| **V2 OData 콜백 패턴** | `sap.ui.model.odata.v2.ODataModel`의 `callFunction`은 Promise를 반환하지 않음. `success`/`error` 콜백 필수 |
| **리뷰 등록 후 서버 재조회** | 수동 배열 조작(prepend + avg 재계산) 대신 `_loadDetail()`로 서버에서 전체 데이터를 다시 가져옴. 사용자명 expand 누락, 평점 계산 오류를 근본적으로 방지 |

---

## 1. detailPage — 컨텐츠 상세 (기능2)

### 1.1 현재 구조

```
sysmgt_ott/webapp/detailPage/webapp/
├── i18n/
│   ├── i18n.properties          # 한국어 기본 (53키)
│   ├── i18n_en.properties       # 영문
│   └── i18n_ko.properties       # 한글 복사본
├── controller/
│   ├── BaseController.js         # (library_ott 상속)
│   └── DetailMain.controller.js # ★ 주 컨트롤러
├── view/
│   ├── App.view.xml              # 루트 (App 컨테이너)
│   └── DetailMain.view.xml      # ★ 주 뷰
├── manifest.json
├── index.html
└── css/style.css
```

### 1.2 뷰 구조 (DetailMain.view.xml)

```
uxap:ObjectPageLayout          ← 뷰 루트 (SAP 표준)
├── headerTitle
│   └── ObjectPageDynamicHeaderTitle
│       ├── heading: Title (컨텐츠명)
│       ├── expandedContent: [컨텐츠타입] ★★★★★ (리뷰 N개)
│       ├── snappedContent: [컨텐츠타입] (스크롤 시 축소)
│       └── actions: [← 뒤로가기]
├── Section: "기본 정보"
│   └── Card/CSS Grid 패널 (.detailInfoCard)
│       ├── [컨텐츠 식별] H6
│       │   ├── 컨텐츠 코드: C001
│       │   └── 유형: MOVIE
│       ├── [제공 정보] H6
│       │   ├── 제공사: Studio Alpha (partner/name via expand)
│       │   └── 등록일: 2026-07-02 (createdAt)
│       ├── [분류] H6
│       │   ├── 장르: ObjectStatus 리스트 (state="None" → 검정색)
│       │   └── 태그: ObjectStatus 리스트
│       └── [📥 기본정보 내보내기] 버튼
├── Section: "출연진"
│   ├── sap.ui.table.Table (visibleRowCount 동적: min(데이터길이, 15))
│   │   ├── 이름 | 역할 | 캐릭터명
│   │   └── OverflowToolbar: 검색 결과 (N)
│   └── [📥 출연진 내보내기] 버튼
├── Section: "리뷰"
│   ├── MessageStrip ★ 최상단 (HasReviewed && !editing 시)
│   │   └── "이미 리뷰를 작성하셨습니다"
│   ├── SubSection: 리뷰 목록
│   │   └── List + CustomListItem (별점·내용·날짜·[✏️수정] [🗑️삭제])
│   └── SubSection: 리뷰 작성 (!HasReviewed || 수정모드 시)
│       └── SimpleForm: 별점 + TextArea + [등록/수정완료] [취소]
```

### 1.3 기본 정보 Card/CSSGrid 설계

**벤치마킹**: SAP BTP Destination Detail 페이지 패턴

```css
.detailInfoCard {
    /* f:Card의 대체 — UI5 1.136 CDN은 f:Card default aggregation 미지원 */
    background: #fff;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    padding: 1rem;
}
.detailCardGrid {
    /* sap.ui.layout.cssgrid의 대체 — UI5 1.136 CDN 미제공 */
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(max(220px, 50%), 1fr));
    gap: 1rem;
}
```

| 필드 | 바인딩 | 출처 |
|------|-------|------|
| 컨텐츠 코드 | `{viewModel>/Detail/content_id}` | Contents.content_id |
| 유형 | `{viewModel>/Detail/content_type}` | Contents.content_type |
| 제공사 | `{viewModel>/Detail/partner/name}` | Partners.name (expand) |
| 등록일 | `{viewModel>/Detail/createdAt}` | managed.createdAt |
| 장르 | `{viewModel>/Detail/genres/results}` | ContentGenre → Genre |
| 태그 | `{viewModel>/Detail/tags/results}` | ContentTag → Tag |

### 1.4 백엔드 API

| 용도 | URL | 방식 |
|------|-----|------|
| 컨텐츠 상세 | `GET /srv-api/odata/v2/detail/Contents('{id}')?$expand=partner,genres($expand=genre),tags($expand=tag),casts,reviews($expand=user)` | `readOdata` |
| 리뷰 등록 | `POST /submitReview` (content_id, rating, review_text) | `callFunction` |
| 리뷰 삭제 | `POST /deleteReview` (review_id) | `callFunction` |

> ⚠️ `partner` expand는 `DetailService.cds`에 `entity Partners`가 명시적으로 노출되어 있어야 함.

### 1.5 핵심 로직

#### A. 1인 1리뷰 제한

```javascript
// UserSessionLoader가 비동기로 Core에 User 모델 등록 → 최대 5회(1초) 재시도 후 체크
// ★ Controller 파괴 시 _bDestroyed 가드로 중단
_checkMyReview: function () {
    if (this._bDestroyed) return;
    var oUserModel = sap.ui.getCore().getModel("User");
    if (!oUserModel) {
        this._checkRetryCount = (this._checkRetryCount || 0) + 1;
        if (this._checkRetryCount <= 5) {
            this._checkTimer = setTimeout(this._checkMyReview.bind(this), 200);
        }
        return;
    }
    this._checkRetryCount = 0;
    var myUserId = oUserModel.getProperty("/user_id");  // 스네이크 케이스!
    // ... 리뷰 목록 순회하여 본인 리뷰 찾기 → /HasReviewed 설정
}
```

- User 모델 키: `/user_id` (NOT `/userId`)
- 뷰 바인딩: `{User>/user_id}`
- `_onRouteMatched`에서 `_checkRetryCount = 0` 명시적 초기화
- ★ `onExit()`에서 `clearTimeout(this._checkTimer)`로 타이머 해제

#### B. 리뷰 등록 → 서버 재조회

```javascript
// AS-IS (버그): 수동 배열 조작 — 사용자명 누락, 평점 계산 오류
// TO-BE: _loadDetail(contentId)로 서버에서 전체 데이터 재조회
// → 사용자명 정상 표시, avg_rating 정확, 코드 40줄 감소
onSubmitReview: function () {
    if (this._bSubmitting) return;  // ★ 중복 제출 방지
    this._bSubmitting = true;
    // ...
    oModel.callFunction("/submitReview", {
        success: function () {
            oViewModel.setProperty("/NewReview", { rating: 0, review_text: "" });
            that._loadDetail(contentId);  // 서버 재조회
            that._bSubmitting = false;
        }
    });
}
```

#### C. 수정 모드 (삭제 → 재등록)

```javascript
// 수정: 기존 리뷰 deleteReview → submitReview 순차 실행
// editing_review_id가 있으면 작성 폼 visible + 타이틀 "리뷰 수정" + "수정 완료" 버튼
// 취소 시 폼 초기화
```

#### D. 리뷰 삭제 — MessageBox.warning 경고

```javascript
// 모든 삭제 지점에 MessageBox.warning으로 확인 (Fiori 표준)
// onDeleteReview + onDeleteMyReview 둘 다 동일한 MessageBox.warning 패턴 사용
MessageBox.warning(sMessage, {
    title: sTitle,
    actions: [MessageBox.Action.OK, MessageBox.Action.CANCEL],
    emphasizedAction: MessageBox.Action.OK,
    onClose: function (sAction) {
        if (that._bDestroyed || sAction !== MessageBox.Action.OK) return;  // ★ 파괴 가드
        // 삭제 실행 → _loadDetail 재조회
    }
});
```

#### E. 엑셀 다운로드

| 버튼 | 데이터 | API |
|------|--------|-----|
| 기본정보 내보내기 | 제목·종류·평점·리뷰수·장르·태그 | `sap/ui/export/Spreadsheet` |
| 출연진 내보내기 | 이름·역할·캐릭터명 | `sap/ui/export/Spreadsheet` |

#### F. Controller 생명주기 안정화

```javascript
onExit: function () {
    this._bDestroyed = true;
    if (this._checkTimer) {
        clearTimeout(this._checkTimer);
        this._checkTimer = null;
    }
}
```

- 모든 비동기 콜백(success/error/onClose)에서 `_bDestroyed` 가드
- `_loadDetail`에 `_loadToken` 토큰 패턴으로 Race Condition 방지

### 1.6 해결된 주요 이슈

| # | 이슈 | 원인 | 해결 |
|---|------|------|------|
| 1 | **무한 스크롤** (스크롤바 계속 증가) | `DynamicPage` 안에 `ObjectPageLayout` 중첩 → spacer div 무한 확장 | `ObjectPageLayout`을 루트로 변경 |
| 2 | **별점 클릭 시 원상 복귀** | `NewReview` 객체가 ViewModel에 초기화되지 않음 | `_onRouteMatched`에서 `{rating:0, review_text:""}` 초기화 |
| 3 | **별점 클릭 시 입력 텍스트 소멸** | 동일한 `NewReview` 경로 의존성 | 초기화로 해결 (동일 원인) |
| 4 | **V2 `callFunction` `.then` 오류** | `sap.ui.model.odata.v2.ODataModel`은 Promise 미반환 | `success`/`error` 콜백 패턴으로 전면 교체 |
| 5 | **User 모델 null (hasUserModel: false)** | `UserSessionLoader` 비동기 로딩이 페이지 로드보다 늦음 | 최대 5회 200ms 간격 재시도 + `_checkRetryCount` 초기화 |
| 6 | **User 모델 `userId` undefined** | 프로퍼티명이 `user_id`(스네이크)인데 `userId`(카멜)로 접근 | `getProperty("/user_id")`로 수정 |
| 7 | **리뷰 등록 후 UI 미갱신** | V2 `callFunction` Promise 미지원으로 callback 미실행 | callback → `_loadDetail` 서버 재조회 |
| 8 | **중복 리뷰 허용** | `HasReviewed` 체크 로직 부재 | `_checkMyReview()` + 작성 폼 `visible` 조건 |
| 9 | **리뷰 섹션 드롭다운 빈 항목** | SubSection에 title 미지정 | `label.reviewList` 추가 |
| 10 | **검색 결과 "[object Object]"** | 배열을 직접 바인딩 | expression binding: `{= … .length}` |
| 11 | **i18n 전면 부재** | 모든 텍스트 하드코딩 | 53개 키 생성, 뷰·컨트롤러 전면 교체 |
| 12 | **flat 레이아웃** | `DynamicPage` + div 나열 | `ObjectPageLayout` + `ObjectPageSection` + `SimpleForm` + `ResponsiveGridLayout` + `GridData span` |
| 13 | **Excel 멀티시트 미지원** | UI5 1.136 CDN `Spreadsheet`가 `sheets` 미지원 | 단일시트 → 2개 버튼 분리 |
| 14 | **리뷰 등록 후 사용자명 빈칸 + 평점 오류** | 수동 배열 조작 시 user expand 누락, avg 계산 버그 | `_loadDetail()` 서버 재조회로 대체 |
| 15 | **평균 평점이 전혀 다른 값 (4.0→3.0이어야 함)** | 🔥 SQL `WHERE content_id = ?` → 실제 FK 컬럼명은 `content_content_id`. 필터링 실패로 전체 리뷰 AVG 반영 | `content_content_id`로 수정 (`DetailService.handler.ts`) |
| 16 | **제공사 정보 출력 불가** | `DetailService.cds`에 `entity Partners` 미노출 | `Partners` 엔티티 추가 + OData expand에 `partner` 추가 |
| 17 | **`_loadDetail` Race Condition** | C001→C002 빠른 내비게이션 시 stale 응답이 최신 데이터 덮어씀 | `_loadToken` 토큰 패턴 |
| 18 | **Controller 파괴 후 콜백 접근** | setTimeout/MessageBox 콜백이 파괴된 Controller 참조 | `onExit()` + `_bDestroyed` 가드 |
| 19 | **리뷰 제출 중복 클릭** | Submit 연타 시 deleteReview→submitReview 체인 꼬임 | `_bSubmitting` 플래그 |

---

## 2. myPage — 마이페이지 (기능4)

### 2.1 현재 구조

```
sysmgt_ott/webapp/myPage/webapp/
├── i18n/
│   ├── i18n.properties          # 한국어 기본 (dialog 키 재편)
│   └── i18n_en.properties       # 영문
├── controller/
│   ├── BaseController.js         # (library_ott 상속)
│   ├── MyMembership.controller.js # ★ 멤버십 조회·해지·재개
│   └── PlanChange.controller.js   # ★ 플랜 선택·구독·변경
├── view/
│   ├── App.view.xml               # 루트
│   ├── MyMembership.view.xml      # ★ 멤버십 카드
│   └── PlanChange.view.xml        # ★ 플랜 선택 카드
├── manifest.json
└── index.html
```

### 2.2 뷰 구조

#### MyMembership.view.xml

```
uxap:ObjectPageLayout
├── headerTitle
│   └── ObjectPageDynamicHeaderTitle
│       ├── heading: {user_name} 님 + ObjectStatus(상태 배지)
│       └── snappedContent: ObjectStatus(상태) (스크롤 축소)
├── Section: "멤버십 정보"
│   ├── SubSection: SimpleForm (ResponsiveGridLayout, 2열 flex 정렬)
│   │   ├── 플랜 | 월 요금
│   │   └── 동시시청 | 기간
│   └── SubSection: OverflowToolbar
│       ├── [플랜 변경] [해지]       ← active 상태
│       ├── [구독 재개]              ← cancelled/suspended
│       └── [구독 시작]              ← none 상태
```

#### PlanChange.view.xml

```
Page (뒤로가기 버튼)
└── VBox
    ├── FlexBox (wrap="Wrap", 카드 그리드)
    │   └── VBox (플랜 카드) × N
    │       ├── Title: 플랜명
    │       ├── Text: 가격 (formatter)
    │       ├── Text: 동시시청 (formatter)
    │       └── Button: [현재 플랜] | [선택/✓ 선택됨]
    └── OverflowToolbar
        └── [구독 시작 / 플랜 변경]
```

### 2.3 아키텍처 의도 — MyMembership ↔ PlanChange 분리

| 설계 | 이유 |
|------|------|
| **별도 라우트** | 조회(MyMembership)와 액션(PlanChange) 분리는 SAP Fiori 표준 패턴 |
| **상태 의존성 분리** | `none`은 "구독 시작", `active`는 "플랜 변경" — 한 화면에서 4개 상태 분기는 복잡도 폭증 |
| **내비게이션 흐름** | 선택 → 취소 → 뒤로가기 자연스러움 |
| **UI 집중도** | MyMembership은 정보 소비, PlanChange는 의사결정에 집중 |

### 2.4 백엔드 API

| 용도 | URL | 방식 |
|------|-----|------|
| 내 멤버십 | `GET /srv-api/odata/v2/ott/Mypage/UserSet?$expand=plan` | `readOdata` |
| 플랜 목록 | `GET /srv-api/odata/v2/ott/Mypage/PlanSet` | `readOdata` |
| 구독 시작 | `POST /subscribe` (plan) | `callFunction` |
| 플랜 변경 | `POST /changePlan` (plan) | `callFunction` |
| 해지 | `POST /cancel` | `callFunction` |
| 재개 | `POST /resume` | `callFunction` |

### 2.5 Formatter 유틸리티

| 함수 | 모듈 | 용도 | 예시 출력 |
|------|------|------|----------|
| `formatUserName` | MyMembership | 이름 + 존칭 | "김민준 님" |
| `formatPrice` | MyMembership | 가격 + 화폐단위 | "9,900원" |
| `formatMaxStreamsText` | MyMembership | 인원 + 단위 | "동시시청 4명" |
| `statusFormatter` | MyMembership | 상태 코드 → 한글 | "active" → "구독중" |
| `statusStateFormatter` | MyMembership | 상태 코드 → 색상 | "active" → "Success" |
| `formatPrice` | PlanChange | 가격 + 단위 | "9,900원/월" |
| `formatMaxStreams` | PlanChange | 인원 + 단위 | "동시시청 4명" |

### 2.6 해결된 주요 이슈

| # | 이슈 | 원인 | 해결 |
|---|------|------|------|
| 1 | **V2 `callFunction` `.then` 오류** | `ODataModel` V2는 Promise 미지원 | `success`/`error` 콜백 전환 (3곳) |
| 2 | **PlanChange 화면 미표시** | `layout:GridBox`가 UI5 1.136 CDN에 없음 | `FlexBox` (`wrap="Wrap"`) |
| 3 | **`wrap="true"` 경고** | `FlexBox.wrap`은 enum | `wrap="Wrap"` |
| 4 | **`borderDesign` 경고** | `VBox`에 없는 속성 | 제거 |
| 5 | **`core:CustomData` 위치 오류** | 직접 자식으로 불가 | `<customData>` 태그로 감쌈 |
| 6 | **i18n 전면 부재** | 모든 텍스트 하드코딩 | dialog 구조 재편 포함 키 생성 |
| 7 | **`DynamicPage` 래퍼** | 무한 스크롤 위험 | `ObjectPageLayout` 루트로 변경 |
| 8 | **멤버십 폼 2열 높이 불균형** | `ResponsiveGridLayout`이 `float` 기반이라 셀 높이 차이 시 배치 이탈 | CSS `flex` 강제 전환 + `align-items: flex-start` |
| 9 | **구독 해지/재개 팝업 아이콘·텍스트 짤림** | `MessageBox.confirm`은 아이콘 '?' 고정, width 제한 | ★ `MessageBox.warning`으로 표준화 → 아이콘⚠️, `contentWidth: "30rem"`, 짧은 제목 + 긴 본문 구조 |
| 10 | **Dialog 메모리 누수** | 커스텀 `Dialog` 사용 시 `destroy()` 누락 | `MessageBox.warning`으로 교체하여 자동 생명주기 관리 + `_bDestroyed` 가드 |
| 11 | **Controller 파괴 후 MessageBox 콜백 접근** | `onClose` 콜백이 파괴된 Controller 참조 | `onExit()` + `_bDestroyed` 가드 |
| 12 | **플랜 변경 중복 제출** | Submit 연타 시 중복 API 호출 | `_bSubmitting` 플래그 |

---

## 3. SAP 표준 UI 패턴 적용 내역

### 3.1 템플릿 벤치마킹 대상

`sysmgt/webapp/` 내 6개 템플릿 모듈:
- userManagement (가장 완성도 높음)
- roleGroupManagement
- codeManagement
- menuManagement
- messageManagement
- roleManagement

### 3.2 적용된 SAP 권장 패턴

| 패턴 | detailPage | myPage | 출처 템플릿 |
|------|:----------:|:------:|------------|
| `ObjectPageLayout` + `ObjectPageDynamicHeaderTitle` | ✅ | ✅ | userManagement DetailList |
| `SimpleForm` + `ResponsiveGridLayout` | ✅ | ✅ | userManagement |
| `GridData span="Lx Mx Sx"` | ✅ | ✅ | 전체 |
| `snappedContent` (스크롤 축소) | ✅ | ✅ | 전체 |
| `OverflowToolbar` | ✅ | ✅ | 전체 |
| `ObjectStatus` (state + icon) | ✅ | ✅ | userManagement |
| `MessageStrip` (안내 배너) | ✅ | — | userManagement |
| `MessageBox.warning` (확인 다이얼로그) | ✅ | ✅ | Fiori 디자인 가이드라인 |
| `sap.ui.table.Table` (출연진) | ✅ | — | — |
| `Card + CSS Grid` (정보 패널) | ✅ | — | SAP BTP Destination Detail 벤치마킹 |
| `FlexBox` (반응형 카드) | — | ✅ | — |
| i18n 리소스 번들 | ✅ (53키) | ✅ (dialog 재편) | 전체 |

### 3.3 의도적으로 적용하지 않은 패턴과 이유

| 패턴 | 미적용 사유 |
|------|-----------|
| `f:DynamicPage` + `ObjectPageLayout` 중첩 | 스크롤 컨테이너 이중화 → spacer 무한 확장 버그 |
| `layout:GridBox` | UI5 1.136 CDN에서 `sap/ui/layout/GridBox.js` 로드 실패 |
| `f:Card` (기본 정보) | UI5 1.136 CDN에서 default aggregation 미지원 → CSS `.detailInfoCard`로 구현 |
| `sap.ui.layout.cssgrid` | UI5 1.136 CDN 미제공 → 순수 CSS Grid (`display: grid`)로 대체 |

---

## 4. 공통 인프라 변경사항

### 4.1 core_ott + library_ott 생성 (2026-06-23)

| 작업 | 파일 수 | 내용 |
|------|:------:|------|
| `core_ott` 신규 생성 | 37 | CDS 14종 + 서비스 4종 + CSV 5종 + 설정 8종 |
| `library_ott` 복사 + API 경로 변경 | 42+7 | `core/` → `ott-core/` 전면 교체 |
| `portal_ott` 참조 변경 | 7 | `common.lib` → `common_ott.lib` |
| `sysmgt_ott` 참조 변경 | 10 | `common.lib` → `common_ott.lib` |
| `approuter_ott` 라우팅 수정 | 5 | `onOttCoreService`(8084) + `onOttService`(8083) 분기 |
| `library` 원본 롤백 | 7 | 템플릿 보호 |

**치명적 버그**: 원래 `xs-app.json`의 catch-all `(.*)`이 `onCapService`로 보내 OTT 비즈니스 서비스 접근 불가. `onOttService`로 변경하여 해결.

### 4.2 approuter_ott 라우팅 (최종)

```
/srv-api/ott-core/* → onOttCoreService (port 8084)  ← OTT 공통 백엔드
/srv-api/*          → onOttService     (port 8083)  ← OTT 비즈니스 백엔드
/common_ott.lib/*   → library_ott
/portal_ott/*       → portal_ott
/sysmgt_ott/*       → sysmgt_ott (정적 파일)
(.*)                → portal_ott (기본)
```

### 4.3 V2 OData 콜백 패턴 — 전체 적용

`sap.ui.model.odata.v2.ODataModel.callFunction()`은 **Promise를 반환하지 않는다**. 모든 `callFunction` 호출을 `success`/`error` 콜백으로 통일:

```javascript
// ❌ 잘못된 패턴
oModel.callFunction("/action", { method: "POST", urlParameters: {...} })
    .then(function() {...}).catch(function() {...});

// ✅ 올바른 패턴 (V2)
oModel.callFunction("/action", {
    method: "POST",
    urlParameters: {...},
    success: function(oData) { /* 처리 */ },
    error: function(oErr) { /* 처리 */ }
});
```

**적용 위치**: detailPage 3곳 (`onSubmitReview`, `onDeleteReview`, `onDeleteMyReview`), myPage 3곳 (`_callAction`, `onSubmitPlan`)

---

## 5. i18n 국제화

### 5.1 detailPage 키 구성 (53개)

| 카테고리 | 키 | 개수 |
|---------|-----|:--:|
| 공통 | `appTitle.*` | 1 |
| 섹션 | `section.*` | 1 |
| 버튼 | `button.*` | 8 |
| 레이블 | `label.*` | 17 |
| 카드 그룹 | `card.*` | 3 |
| 메시지 | `message.*` | 12 |
| Placeholder | `placeholder.*` | 1 |
| 단위 | `label.countUnit` | 1 |
| 내보내기 | `export.*` | 4 |
| 검색 | `label.searchResults` | 1 |

**신규 키 (7/2)**:
```
label.contentId=컨텐츠 코드 / Content Code
label.contentType=유형 / Type
label.partner=제공사 / Provider
label.createdDate=등록일 / Registered Date
card.identification=컨텐츠 식별 / Identification
card.providerInfo=제공 정보 / Provider Information
card.classification=분류 / Classification
```

### 5.2 myPage 키 구성 (dialog 키 재편)

| 카테고리 | 키 | 개수 |
|---------|-----|:--:|
| 공통 | `appTitle.*` | 2 |
| 섹션 | `section.*` | 1 |
| 버튼 | `button.*` | 7 |
| 레이블 | `label.*` | 9 |
| 상태 | `status.*` | 4 |
| 메시지 | `message.*` | 11 |
| 대화상자 | `dialog.*` | 4 |
| Placeholder | `placeholder.*` | 1 |

**dialog 키 재편 (7/2)**:
```
// AS-IS
message.cancelConfirm=구독을 해지하시겠어요? (길어서 제목 잘림)
message.resumeConfirm=구독을 재개하시겠어요? (길어서 제목 잘림)

// TO-BE (짧은 제목 + 긴 본문 분리)
dialog.cancelTitle=구독 해지 / Cancel Subscription
dialog.cancelBody=구독을 해지하시겠어요? 해지 후에도 다시 재개하실 수 있습니다. / Do you want to cancel your subscription? You can resume it later.
dialog.resumeTitle=구독 재개 / Resume Subscription
dialog.resumeBody=구독을 재개하시겠어요? / Do you want to resume your subscription?
```

### 5.3 언어 지원

| 언어 | 파일 |
|------|------|
| 한국어 (기본) | `i18n.properties` |
| 한국어 (명시) | `i18n_ko.properties` |
| 영어 | `i18n_en.properties` |

---

## 6. 주요 교훈 (Lessons Learned)

| # | 교훈 | 상세 |
|---|------|------|
| 1 | **_ott 패턴 일관성** | 원본 템플릿은 절대 수정하지 않고 반드시 `_ott` 복사본에 작업 |
| 2 | **V2 OData 콜백 필수** | `callFunction`은 Promise 미지원. `success`/`error` 콜백 사용 |
| 3 | **User 모델 프로퍼티명** | `user_id` (스네이크). `userId` (카멜) 아님. `sap.ui.getCore().getModel("User").getProperty("/user_id")` |
| 4 | **User 모델 비동기 로딩** | `UserSessionLoader`는 비동기. 페이지 로드 시 null 가능 → 재시도 패턴 필요 |
| 5 | **`DynamicPage` + `ObjectPageLayout` 중첩 금지** | 두 컨트롤러 모두 자체 스크롤 관리 → spacer 무한 확장 |
| 6 | **`ObjectPageLayout` 루트 사용** | `ObjectPageDynamicHeaderTitle`만으로 `expandedContent`·`snappedContent`·`actions` 모두 지원 |
| 7 | **UI5 1.136 CDN 제약** | `sap/ui/layout/GridBox.js` 미제공, `f:Card` default aggregation 미지원, `sap.ui.layout.cssgrid` 미제공 → CSS로 우회 |
| 8 | **View ↔ Controller `this` 컨텍스트** | 콜백 내 `this` 보존 위해 `var that = this` 또는 `.bind(this)` 패턴 필수 |
| 9 | **approuter `catch-all` 확인** | `(.*)` destination이 실제 의도한 서비스를 가리키는지 반드시 확인 |
| 10 | **board 벤치마킹** | 작동하는 코드를 1:1 비교하는 것이 가장 빠른 디버깅 방법 |
| 11 | **`useBatch: false`** | V2 OData에서 배치 모드는 불필요한 복잡성만 추가 |
| 12 | **`async: true`** | SAPUI5 1.136에서 비동기 부트스트랩 필수 |
| 13 | **`ResponsiveGridLayout`은 `float` 기반** | 셀 높이 차이 시 배치 이탈. `display: flex`로 강제 전환하여 2열 정렬 안정화 |
| 14 | **`Dialog.close()` ≠ 메모리 해제** | UI5 컨트롤은 `destroy()`를 호출해야 GC 대상이 됨 |
| 15 | **CAP Association FK 컬럼명** | `Association to Foo`는 `foo_ID` 형태의 FK 컬럼 생성. SQL raw query에서 `content_id` → `content_content_id` 실수 방지 |
| 16 | **서버 재조회가 수동 조작보다 안전** | `_loadDetail()` 한 줄이 배열 prepend + avg 재계산 40줄보다 정확하고 버그에 강함 |
| 17 | **`MessageBox.warning` 표준화** | `actions` + `emphasizedAction` + `contentWidth` + `onClose` 패턴으로 Fiori 디자인 가이드라인 준수 |
| 18 | **Controller 생명주기 (`onExit`)** | UI5 SPA에서 페이지 전환 시 Controller 파괴 → `onExit()`에서 타이머 해제, `_bDestroyed` 플래그로 모든 비동기 콜백 가드 |
| 19 | **Race Condition 방지 (토큰 패턴)** | 비동기 요청마다 `_loadToken` 증가 → 응답 도착 시 토큰 비교하여 stale 응답 무시 |
| 20 | **중복 제출 방지** | `_bSubmitting` 플래그로 Submit 버튼 연타 방어 + 성공/실패 콜백에서 복원 |
| 21 | **`fnDeepCopy` 순환 참조** | 재귀적 deep copy 시 `WeakSet`으로 방문 객체 추적 → stack overflow 방지 |
| 22 | **ID 생성기 충돌 방지** | `Date.now()` 기반 ID는 동시 요청 시 충돌 → `crypto.randomUUID()` 사용 |

---

## 7. 전체 파일 변경 이력

### 7.1 2026-06-23: core_ott/library_ott 생성

| # | 작업 | 파일 수 |
|---|------|:--:|
| 1 | core_ott 신규 생성 | 37 |
| 2 | library_ott 신규 생성 | 42 |
| 3 | library_ott API 경로 변경 | 7 |
| 4 | portal_ott 참조 변경 | 7 |
| 5 | sysmgt_ott 참조 변경 | 10 |
| 6 | approuter_ott 라우팅 수정 | 5 |
| 7 | library 원본 롤백 | 7 |
| **총계** | | **115** |

### 7.2 2026-06-25: 프론트엔드 디버깅

| 작업 | 내용 |
|------|------|
| approuter 전환 | fiori-tools-proxy 제거 → approuter 직접 서빙 |
| `router.getView()` 버그 수정 | `this.getView()`로 교체 |
| V2 OData 모델 설정 | `useBatch: false`, named model, trailing slash 제거 |
| `default-env.json` 생성 | destinations 설정 |
| ui5-local.yaml 삭제 | board 방식으로 단순화 |
| Component-preload.js 삭제 | 개발 중 불필요 |

### 7.3 2026-06-26: SAP 권장 프론트엔드 리팩토링

| Phase | 내용 | 파일 |
|-------|------|:--:|
| 1 | i18n 전면 적용 | 6 |
| 2 | detailPage `ObjectPageLayout` 도입 | 3 |
| 3 | detailPage formatter 추가 | 1 |
| 4 | myPage `ObjectPageLayout` 도입 | 3 |
| 5 | PlanChange `GridList` → `FlexBox` | 2 |
| 6 | 리뷰 CRUD 완성 (수정·1인제한·즉시반영) | 3 |
| 7 | 엑셀 다운로드 (기본정보 + 출연진 분리) | 3 |
| 8 | V2 콜백 패턴 전면 적용 | 4 |
| 9 | 무한 스크롤 버그 수정 | 2 |
| 10 | User 모델 비동기 재시도 | 1 |

### 7.4 2026-07-01: UI 피드백 반영 + 런타임 안정성

| # | 내용 | 파일 |
|---|------|:--:|
| 1 | 장르 색상 수정 (`state="Information"` → `"None"`) | 1 |
| 2 | 출연진 테이블 `visibleRowCount` 동적 적용 | 1 |
| 3 | 엑셀 버튼을 각 섹션 하단으로 이동 | 1 |
| 4 | 리뷰 수정/삭제 버튼 중복 제거 | 1 |
| 5 | 멤버십 폼 2열 flex 정렬 (float→flex) | 3 |
| 6 | MessageBox.confirm → 커스텀 Dialog | 3 |
| 7 | Dialog destroy 누락 수정, console.log 제거 | 2 |
| **총계** | | **12** |

### 7.5 2026-07-02: detailPage 기본 정보 확장 + MessageBox 표준화 + 평점 버그 수정

| # | 내용 | 파일 |
|---|------|:--:|
| 1 | 기본 정보 Card/CSSGrid 확장 (content_id, type, partner, createdAt) | 3 |
| 2 | 리뷰 섹션 재구성 (MessageStrip 최상단, "내 리뷰" 제거) | 1 |
| 3 | 리뷰 등록 로직 단순화 (수동 배열 조작 → `_loadDetail` 재조회) | 1 |
| 4 | 리뷰 삭제 MessageBox.warning 경고 추가 | 1 |
| 5 | myPage 구독 Dialog → MessageBox.warning 표준화 | 2 |
| 6 | 평점 SQL 컬럼명 버그 수정 (`content_id` → `content_content_id`) | 1 |
| 7 | Partners 엔티티 노출 (DetailService.cds) | 1 |
| 8 | i18n 신규 키 8개 + dialog 키 재편 | 4 |
| 9 | 장르·태그 칩 폰트 크기 정상화, MessageBox CSS | 2 |
| **총계** | | **11** |

### 7.6 2026-07-03: Controller 생명주기 안정화 + Race Condition 방지

| # | 내용 | 파일 |
|---|------|:--:|
| 1 | `_loadDetail` loadToken 패턴 (Race Condition 방지) | 1 |
| 2 | `onExit()` + `_bDestroyed` 가드 (detailPage) | 1 |
| 3 | setTimeout 핸들 저장 + `clearTimeout` | 1 |
| 4 | `onSubmitReview` 중복 제출 방지 (`_bSubmitting`) | 1 |
| 5 | `onDeleteMyReview` → `MessageBox.warning` 통일 | 1 |
| 6 | MyMembership `onExit()` + `_bDestroyed` 가드 | 1 |
| 7 | PlanChange `onExit()` + `_bDestroyed` + `_bSubmitting` | 1 |
| 8 | `fnDeepCopy` WeakSet 순환 참조 방지 | 1 |
| 9 | `generateReviewId` → `crypto.randomUUID()` | 1 |
| 10 | `_checkRetryCount` 명시적 초기화 | 1 |
| **총계** | | **5** (중복 파일 합산) |

---

## 8. 테스트

### 8.1 기존 테스트 (2026-06-22 기준, 74/74 PASS)

| 폴더 | 테스트 | 케이스 |
|------|--------|:--:|
| `approuter_ott/test/` | `test-routing.mjs` | 11 |
| `portal_ott/test/` | `test-structure.mjs` | 14 |
| `sysmgt_ott/test/` | `test-structure.mjs` | 28 |
| `sysmgt_ott/test/` | `test-apis.mjs` | 21 |

### 8.2 수동 E2E 검증 (2026-07-03 기준)

| 페이지 | 체크 항목 | 결과 |
|--------|----------|:--:|
| detailPage | 스크롤 축소 (`snappedContent`) | ✅ |
| detailPage | 섹션 anchor bar 네비게이션 | ✅ |
| detailPage | 장르 텍스트 검정색 (링크 오인 해소) | ✅ |
| detailPage | 출연진 테이블 동적 행수 | ✅ |
| detailPage | 엑셀 버튼 섹션별 배치 | ✅ |
| detailPage | 기본 정보 Card/CSSGrid (content_id, 유형, 제공사, 등록일) | ✅ |
| detailPage | 리뷰 MessageStrip 최상단, "내 리뷰" 섹션 없음 | ✅ |
| detailPage | 리뷰 등록 후 사용자명 정상 표시 + 평균 별점 정확 | ✅ |
| detailPage | 리뷰 등록·수정·삭제 (MessageBox.warning 경고) | ✅ |
| detailPage | 1인 1리뷰 제한 | ✅ |
| detailPage | 빠른 연속 내비게이션 시 데이터 불일치 없음 (Race Condition) | ✅ |
| detailPage | 리뷰 중복 제출 방지 | ✅ |
| myPage | 멤버십 정보 2열 균일 정렬 | ✅ |
| myPage | 상태별 버튼 분기 | ✅ |
| myPage | 구독 해지/재개 MessageBox.warning (영문 잘림 없음) | ✅ |
| myPage | PlanChange 플랜 카드 | ✅ |
| myPage | 구독·변경·해지·재개 | ✅ |
| myPage | 플랜 변경 중복 제출 방지 | ✅ |

---

> **문서 버전**: v5.0 (최종) · 2026-07-03
> **이전 버전**: v1.0 (2026-06-22) · v2.0 (2026-06-23) · v3.0 (2026-06-26) · v4.0 (2026-07-01)
