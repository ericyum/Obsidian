(에이전트와 오케스트레이터를 어디에 둘 건지에 대한 설명도 있어야한다. main agent를 두는 방안을 고려)

향후에는 main agent(ZEN-AI agent)를 진입점으로 해서 fan-in fan-out 구조로 해서 회의록 뿐만 이 아니라 상황에 따라 다양한 업무를 할 수 있도록 하는 것이 장기적인 목표이다. 마치 pi agent 처럼 말이다.

(상태머신 공부 하기 checkpoint와 state를 지정하기 위해서, graph engineering에 대해서도 한 번더 정독)

모델은 stateless 이다.


state 

너는 누구야?
api 프로토콜 형식도 미리소통함

1:1 이라서 병렬 처리가 안됨.
서버 자원이 그 연결된 사람에게 묶임

그래서 stateless 방식이 도입
한명이 한명을 담당하지 않는 대신 모든 내용을 던진다.(그 형식은 클라이언트에 잇음)
그럼 그 때 비어있는 서버가 받는다.

LLM도 stateless이다. 모델도 모든 것은 받아야 하고 모르기 때문이다.