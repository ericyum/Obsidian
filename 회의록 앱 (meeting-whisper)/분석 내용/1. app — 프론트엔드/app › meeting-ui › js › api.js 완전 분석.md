# api.js 완전 분석

> 소스: `app/meeting-ui/js/api.js`  
> 역할: MeetingService OData action/entity 호출을 fetch 기반 함수로 캡슐화한다.

## 1. 이 파일이 있는 이유

MeetingService OData action/entity 호출을 fetch 기반 함수로 캡슐화한다.

이 파일은 **1. app — 프론트엔드** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `app/meeting-ui/js/api.js` |
| 형식 | `javascript` |
| 전체 줄 수 | 624 |
| 주석 줄 수 | 1 |
| 주요 심볼 수 | 44 |
| 환경 변수 참조 수 | 0 |

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `emitSessionEvent` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `markSessionTouched` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `markSessionExpired` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `isSessionHtmlError` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `apiMode` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `useCapApi` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `useBrowserSessionKeepAlive` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `requestMethod` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `needsCsrf` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `fetchCsrfToken` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `prepareOptions` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `parseJsonResponse` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `requestJson` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `getCurrentUser` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `keepSessionAlive` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `postAction` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `encodeFilter` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `uploadCapAudio` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `uploadBinaryWithFetch` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `binaryUploadHeaders` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `uploadBinaryWithXhr` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `readEntityList` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `readFirst` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `safeJsonParse` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `odataString` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `capStatusToStage` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `capReviewItemToJobItem` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `capJobFromMeeting` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `capGetJob` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `createJob` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `getJob` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `patchSpeakers` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `deleteSpeaker` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `patchSegmentSpeaker` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `patchTranscript` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `patchNote` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `rerunSummary` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `runTranscriptReview` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `retryMeeting` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `listMeetings` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `deleteMeeting` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `patchMeetingTitle` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `patchMeetingCategory` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `patchMeetingAccess` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 6. 경로·엔드포인트 단서

- `/user-api/currentUser`
- `/api/Meetings`
- `/api/jobs`
- `/api/meetings`

## 7. 코드 흐름 상세

### 7.1 `emitSessionEvent`

- 위치: 7~11행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `markSessionTouched`

- 위치: 12~15행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `markSessionExpired`

- 위치: 16~19행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `isSessionHtmlError`

- 위치: 20~23행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `apiMode`

- 위치: 24~38행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `useCapApi`

- 위치: 39~42행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `useBrowserSessionKeepAlive`

- 위치: 43~46행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `requestMethod`

- 위치: 47~50행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `needsCsrf`

- 위치: 51~54행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.10 `fetchCsrfToken`

- 위치: 55~76행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.11 `prepareOptions`

- 위치: 77~95행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.12 `parseJsonResponse`

- 위치: 96~114행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.13 `requestJson`

- 위치: 115~151행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.14 `getCurrentUser`

- 위치: 152~179행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.15 `keepSessionAlive`

- 위치: 180~187행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.16 `postAction`

- 위치: 188~195행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.17 `encodeFilter`

- 위치: 196~201행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.18 `uploadCapAudio`

- 위치: 202~212행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.19 `uploadBinaryWithFetch`

- 위치: 213~253행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.20 `binaryUploadHeaders`

- 위치: 254~267행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.21 `uploadBinaryWithXhr`

- 위치: 268~306행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.22 `readEntityList`

- 위치: 307~316행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.23 `readFirst`

- 위치: 317~321행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.24 `safeJsonParse`

- 위치: 322~330행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.25 `odataString`

- 위치: 331~334행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.26 `capStatusToStage`

- 위치: 335~349행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.27 `capReviewItemToJobItem`

- 위치: 350~361행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.28 `capJobFromMeeting`

- 위치: 362~417행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.29 `capGetJob`

- 위치: 418~424행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.30 `createJob`

- 위치: 425~460행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.31 `getJob`

- 위치: 461~465행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.32 `patchSpeakers`

- 위치: 466~474행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.33 `deleteSpeaker`

- 위치: 475~481행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.34 `patchSegmentSpeaker`

- 위치: 482~495행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.35 `patchTranscript`

- 위치: 496~512행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.36 `patchNote`

- 위치: 513~534행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.37 `rerunSummary`

- 위치: 535~542행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.38 `runTranscriptReview`

- 위치: 543~550행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.39 `retryMeeting`

- 위치: 551~558행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.40 `listMeetings`

- 위치: 559~573행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
