# session.js 완전 분석

> 소스: `app/meeting-ui/js/session.js`  
> 역할: 세션 만료 감지, keep-alive, 탭 간 만료 전파와 업로드 전 세션 확인을 담당한다.

## 1. 이 파일이 있는 이유

세션 만료 감지, keep-alive, 탭 간 만료 전파와 업로드 전 세션 확인을 담당한다.

이 파일은 **1. app — 프론트엔드** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `app/meeting-ui/js/session.js` |
| 형식 | `javascript` |
| 전체 줄 수 | 234 |
| 주석 줄 수 | 2 |
| 주요 심볼 수 | 18 |
| 환경 변수 참조 수 | 0 |

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `initSessionMonitor` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `refreshSession` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `ensureFreshSession` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `startRecordingSessionKeepAlive` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `stopRecordingSessionKeepAlive` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `checkSessionAge` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `touchSession` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `readStoredTouch` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `persistTouch` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `showWarning` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `hideWarning` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `showExpired` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `hideExpired` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `ensureWarningToast` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `ensureExpiredModal` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `setRefreshing` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `formatRemaining` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `stopSessionMonitor` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 7. 코드 흐름 상세

### 7.1 `initSessionMonitor`

- 위치: 25~49행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `refreshSession`

- 위치: 50~73행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `ensureFreshSession`

- 위치: 74~78행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `startRecordingSessionKeepAlive`

- 위치: 79~88행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `stopRecordingSessionKeepAlive`

- 위치: 89~94행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `checkSessionAge`

- 위치: 95~109행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `touchSession`

- 위치: 110~118행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `readStoredTouch`

- 위치: 119~127행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `persistTouch`

- 위치: 128~135행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.10 `showWarning`

- 위치: 136~142행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.11 `hideWarning`

- 위치: 143~146행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.12 `showExpired`

- 위치: 147~158행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.13 `hideExpired`

- 위치: 159~162행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.14 `ensureWarningToast`

- 위치: 163~188행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.15 `ensureExpiredModal`

- 위치: 189~214행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.16 `setRefreshing`

- 위치: 215~221행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.17 `formatRemaining`

- 위치: 222~228행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.18 `stopSessionMonitor`

- 위치: 229~234행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
