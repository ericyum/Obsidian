# ui.js 완전 분석

> 소스: `app/meeting-ui/js/ui.js`  
> 역할: 상태 진행률, 전사, 요약, 회의 목록을 HTML로 변환하는 표현 계층이다.

## 1. 이 파일이 있는 이유

상태 진행률, 전사, 요약, 회의 목록을 HTML로 변환하는 표현 계층이다.

이 파일은 **1. app — 프론트엔드** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `app/meeting-ui/js/ui.js` |
| 형식 | `javascript` |
| 전체 줄 수 | 249 |
| 주석 줄 수 | 0 |
| 주요 심볼 수 | 18 |
| 환경 변수 참조 수 | 0 |

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `show` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `fmtClock` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `fmtDuration` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `fmtSeconds` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `fmtTs` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `displaySpeaker` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `renderSteps` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `renderJobProgress` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `queueStatusText` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `workerStatusText` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `transcriptionProgressText` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `relativeHeartbeatText` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `isWorkerHeartbeatStale` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `renderSummary` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `renderTranscript` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `esc` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `fmtDate` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `renderMeetingList` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 7. 코드 흐름 상세

### 7.1 `show`

- 위치: 3~8행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `fmtClock`

- 위치: 9~14행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `fmtDuration`

- 위치: 15~20행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `fmtSeconds`

- 위치: 21~27행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `fmtTs`

- 위치: 28~33행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `displaySpeaker`

- 위치: 34~42행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `renderSteps`

- 위치: 43~58행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `renderJobProgress`

- 위치: 59~104행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `queueStatusText`

- 위치: 105~121행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.10 `workerStatusText`

- 위치: 122~139행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.11 `transcriptionProgressText`

- 위치: 140~145행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.12 `relativeHeartbeatText`

- 위치: 146~155행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.13 `isWorkerHeartbeatStale`

- 위치: 156~162행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.14 `renderSummary`

- 위치: 163~177행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.15 `renderTranscript`

- 위치: 178~185행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.16 `esc`

- 위치: 186~195행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.17 `fmtDate`

- 위치: 196~203행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.18 `renderMeetingList`

- 위치: 204~249행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
