# meeting-service.cds 완전 분석

> 소스: `cap/srv/meeting-service.cds`  
> 역할: 사용자용 OData 서비스의 projection, complex type, action 계약을 정의한다.

## 1. 이 파일이 있는 이유

사용자용 OData 서비스의 projection, complex type, action 계약을 정의한다.

이 파일은 **3. cap — 데이터와 백엔드** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `cap/srv/meeting-service.cds` |
| 형식 | `cds` |
| 전체 줄 수 | 89 |
| 주석 줄 수 | 0 |
| 주요 심볼 수 | 21 |
| 환경 변수 참조 수 | 0 |

## 3. 의존성

- `../db/schema`

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `MeetingService` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `CleanupResult` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `MeetingStatus` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `Meetings` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `Transcripts` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `Summaries` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `ReviewItems` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `uploadAudio` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `enqueueTranscription` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `retryMeeting` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `getMeetingStatus` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `cleanupExpiredAudio` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `runSummary` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `runReview` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `deleteAudio` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `saveTranscript` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `saveSummary` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `updateMeetingTitle` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `updateMeetingCategory` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `updateMeetingAccess` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `deleteMeeting` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 6. 경로·엔드포인트 단서

- `/api`

## 7. 코드 흐름 상세

### 7.1 `MeetingService`

- 위치: 4~4행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `CleanupResult`

- 위치: 5~10행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `MeetingStatus`

- 위치: 11~48행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `Meetings`

- 위치: 49~49행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `Transcripts`

- 위치: 50~50행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `Summaries`

- 위치: 51~51행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `ReviewItems`

- 위치: 52~53행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `uploadAudio`

- 위치: 54~60행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `enqueueTranscription`

- 위치: 61~61행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.10 `retryMeeting`

- 위치: 62~62행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.11 `getMeetingStatus`

- 위치: 63~64행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.12 `cleanupExpiredAudio`

- 위치: 65~65행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.13 `runSummary`

- 위치: 66~66행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.14 `runReview`

- 위치: 67~67행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.15 `deleteAudio`

- 위치: 68~68행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.16 `saveTranscript`

- 위치: 69~74행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.17 `saveSummary`

- 위치: 75~79행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.18 `updateMeetingTitle`

- 위치: 80~80행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.19 `updateMeetingCategory`

- 위치: 81~81행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.20 `updateMeetingAccess`

- 위치: 82~86행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.21 `deleteMeeting`

- 위치: 87~89행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
