# meeting-service.js 완전 분석

> 소스: `cap/srv/meeting-service.js`  
> 역할: 소유권, 업로드, dispatch, 상태 조회, 요약·검토, 편집·삭제의 실제 비즈니스 로직이다.

## 1. 이 파일이 있는 이유

소유권, 업로드, dispatch, 상태 조회, 요약·검토, 편집·삭제의 실제 비즈니스 로직이다.

이 파일은 **3. cap — 데이터와 백엔드** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `cap/srv/meeting-service.js` |
| 형식 | `javascript` |
| 전체 줄 수 | 761 |
| 주석 줄 수 | 0 |
| 주요 심볼 수 | 14 |
| 환경 변수 참조 수 | 0 |

## 3. 의존성

- `@sap/cds`
- `./lib/audio-store`
- `./lib/ai-core`
- `./lib/meeting-note`
- `./lib/status`
- `./lib/cleanup`
- `./lib/worker-client`
- `./lib/transcription-dispatcher`

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `action: uploadAudio` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: enqueueTranscription` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: retryMeeting` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: getMeetingStatus` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: cleanupExpiredAudio` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: runSummary` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: runReview` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: deleteAudio` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: saveTranscript` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: saveSummary` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: updateMeetingTitle` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: updateMeetingCategory` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: updateMeetingAccess` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `action: deleteMeeting` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 7. 코드 흐름 상세

### 7.1 `CREATE`

- 위치: 493~513행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `READ`

- 위치: 514~518행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `UPDATE`

- 위치: 519~523행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `DELETE`

- 위치: 524~527행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `UPDATE`

- 위치: 528~541행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `READ`

- 위치: 542~543행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `uploadAudio`

- 위치: 544~560행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `enqueueTranscription`

- 위치: 561~568행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `retryMeeting`

- 위치: 569~579행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.10 `getMeetingStatus`

- 위치: 580~585행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.11 `cleanupExpiredAudio`

- 위치: 586~590행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.12 `runSummary`

- 위치: 591~618행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.13 `runReview`

- 위치: 619~641행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.14 `deleteAudio`

- 위치: 642~650행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.15 `saveTranscript`

- 위치: 651~687행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.16 `saveSummary`

- 위치: 688~711행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.17 `updateMeetingTitle`

- 위치: 712~719행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.18 `updateMeetingCategory`

- 위치: 720~728행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.19 `updateMeetingAccess`

- 위치: 729~746행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.20 `deleteMeeting`

- 위치: 747~761행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
