# worker-internal-service.cds 완전 분석

> 소스: `cap/srv/worker-internal-service.cds`  
> 역할: Worker가 claim, heartbeat, audio 조회, 결과 저장, 실패 보고에 쓰는 내부 계약이다.

## 1. 이 파일이 있는 이유

Worker가 claim, heartbeat, audio 조회, 결과 저장, 실패 보고에 쓰는 내부 계약이다.

이 파일은 **3. cap — 데이터와 백엔드** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `cap/srv/worker-internal-service.cds` |
| 형식 | `cds` |
| 전체 줄 수 | 70 |
| 주석 줄 수 | 0 |
| 주요 심볼 수 | 13 |
| 환경 변수 참조 수 | 0 |

## 3. 의존성

- `../db/schema`

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `WorkerInternalService` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `AudioPayload` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `Meetings` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `AudioObjects` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `Transcripts` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `Summaries` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `ReviewItems` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `claimTranscription` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `heartbeatTranscription` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `getAudio` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `saveTranscript` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `logAiUsage` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `failTranscription` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 6. 경로·엔드포인트 단서

- `/internal`

## 7. 코드 흐름 상세

### 7.1 `WorkerInternalService`

- 위치: 4~4행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `AudioPayload`

- 위치: 5~15행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `Meetings`

- 위치: 16~16행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `AudioObjects`

- 위치: 17~17행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `Transcripts`

- 위치: 18~18행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `Summaries`

- 위치: 19~19행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `ReviewItems`

- 위치: 20~21행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `claimTranscription`

- 위치: 22~25행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `heartbeatTranscription`

- 위치: 26~32행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.10 `getAudio`

- 위치: 33~33행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.11 `saveTranscript`

- 위치: 34~39행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.12 `logAiUsage`

- 위치: 40~63행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.13 `failTranscription`

- 위치: 64~70행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
