# migrate-legacy-sqlite-to-hana.js 완전 분석

> 소스: `scripts/migrate-legacy-sqlite-to-hana.js`  
> 역할: 개발, 배포, 점검 또는 운영 복구 절차를 자동화하는 실행 스크립트다.

## 1. 이 파일이 있는 이유

개발, 배포, 점검 또는 운영 복구 절차를 자동화하는 실행 스크립트다.

이 파일은 **6. scripts — 자동화와 운영 도구** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `scripts/migrate-legacy-sqlite-to-hana.js` |
| 형식 | `javascript` |
| 전체 줄 수 | 264 |
| 주석 줄 수 | 0 |
| 주요 심볼 수 | 11 |
| 환경 변수 참조 수 | 1 |

## 3. 의존성

- `node:child_process`
- `node:path`
- `node:crypto`
- `@sap/cds`
- `../cap/srv/lib/meeting-note`
- `json`
- `sqlite3`
- `sys`

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `argValue` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `hasFlag` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `pythonExe` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `readLegacyRows` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `capStatus` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `safeJson` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `normalizeReviewItem` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `isUuid` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `uuidFromLegacyId` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `toMeeting` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `main` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 5. 환경 변수

- `PYTHON`: 배포 환경과 로컬 실행 값이 달라질 수 있으므로 `mta.yaml`, `.env.example`과 함께 확인한다.

## 7. 코드 흐름 상세

### 7.1 `argValue`

- 위치: 9~14행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `hasFlag`

- 위치: 15~18행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `pythonExe`

- 위치: 19~22행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `readLegacyRows`

- 위치: 23~68행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `capStatus`

- 위치: 69~76행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `safeJson`

- 위치: 77~86행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `normalizeReviewItem`

- 위치: 87~100행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `isUuid`

- 위치: 101~104행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `uuidFromLegacyId`

- 위치: 105~113행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.10 `toMeeting`

- 위치: 114~139행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.11 `main`

- 위치: 140~264행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
