# test_models.py 완전 분석

> 소스: `tests/test_models.py`  
> 역할: 해당 기능의 정상·오류·경계 조건을 고정하는 자동화 테스트다.

## 1. 이 파일이 있는 이유

해당 기능의 정상·오류·경계 조건을 고정하는 자동화 테스트다.

이 파일은 **7. tests — 검증 체계** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `tests/test_models.py` |
| 형식 | `python` |
| 전체 줄 수 | 64 |
| 주석 줄 수 | 0 |
| 주요 심볼 수 | 9 |
| 환경 변수 참조 수 | 0 |

## 3. 의존성

- `app.models`

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `test_transcript_roundtrip` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `test_meeting_note_defaults` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `test_action_item_optional_due` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `test_job_defaults` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `test_job_v2_fields_defaults` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `test_job_duration_default` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `test_job_processing_seconds_default` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `test_job_v2_fields_roundtrip` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `test_job_created_at_default` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 7. 코드 흐름 상세

### 7.1 `test_transcript_roundtrip`

- 위치: 4~15행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `test_meeting_note_defaults`

- 위치: 16~20행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `test_action_item_optional_due`

- 위치: 21~25행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `test_job_defaults`

- 위치: 26~32행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `test_job_v2_fields_defaults`

- 위치: 33~40행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `test_job_duration_default`

- 위치: 41~45행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `test_job_processing_seconds_default`

- 위치: 46~50행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `test_job_v2_fields_roundtrip`

- 위치: 51~60행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `test_job_created_at_default`

- 위치: 61~64행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
