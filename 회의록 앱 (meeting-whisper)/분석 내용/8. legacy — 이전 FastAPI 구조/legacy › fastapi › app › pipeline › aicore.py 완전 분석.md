# aicore.py 완전 분석

> 소스: `legacy/fastapi/app/pipeline/aicore.py`  
> 역할: 현재 CAP 중심 경로 이전의 FastAPI 단일 애플리케이션 구현을 보존한 코드다.

## 1. 이 파일이 있는 이유

현재 CAP 중심 경로 이전의 FastAPI 단일 애플리케이션 구현을 보존한 코드다.

이 파일은 **8. legacy — 이전 FastAPI 구조** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `legacy/fastapi/app/pipeline/aicore.py` |
| 형식 | `python` |
| 전체 줄 수 | 566 |
| 주석 줄 수 | 9 |
| 주요 심볼 수 | 23 |
| 환경 변수 참조 수 | 0 |

## 3. 의존성

- `__future__`
- `json`
- `logging`
- `os`
- `random`
- `re`
- `time`
- `ast`
- `dataclasses`
- `typing`

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `AICoreConfigError` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `AICoreCompletionError` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `EmptyResponseError` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_Message` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_Choice` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_CompletionResponse` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_ChatCompletions` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_Chat` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `AICoreOrchestrationClient` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `make_aicore_client` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_direct_config` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_run_orchestration` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_response_content` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_placeholder_values` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_clean` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_placeholder_allowlist` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_seed_env` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_status_code` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_is_retryable` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_retry_after_seconds` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_unused_parameters` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_wait_seconds` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_looks_like_auth_failure` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 7. 코드 흐름 상세

### 7.1 `AICoreConfigError`

- 위치: 25~28행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `AICoreCompletionError`

- 위치: 29~32행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `EmptyResponseError`

- 위치: 33~39행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `_Message`

- 위치: 40~44행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `_Choice`

- 위치: 45~50행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `_CompletionResponse`

- 위치: 51~54행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `_ChatCompletions`

- 위치: 55~75행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `_Chat`

- 위치: 76~80행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `AICoreOrchestrationClient`

- 위치: 81~284행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.10 `make_aicore_client`

- 위치: 285~310행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.11 `_direct_config`

- 위치: 311~379행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.12 `_run_orchestration`

- 위치: 380~418행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.13 `_response_content`

- 위치: 419~428행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.14 `_placeholder_values`

- 위치: 429~469행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.15 `_clean`

- 위치: 470~476행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.16 `_placeholder_allowlist`

- 위치: 477~483행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.17 `_seed_env`

- 위치: 484~490행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.18 `_status_code`

- 위치: 491~511행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.19 `_is_retryable`

- 위치: 512~520행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.20 `_retry_after_seconds`

- 위치: 521~535행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.21 `_unused_parameters`

- 위치: 536~550행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.22 `_wait_seconds`

- 위치: 551~559행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.23 `_looks_like_auth_failure`

- 위치: 560~566행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
