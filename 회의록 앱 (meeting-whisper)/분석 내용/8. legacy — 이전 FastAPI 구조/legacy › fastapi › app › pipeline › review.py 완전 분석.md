# review.py 완전 분석

> 소스: `legacy/fastapi/app/pipeline/review.py`  
> 역할: 현재 CAP 중심 경로 이전의 FastAPI 단일 애플리케이션 구현을 보존한 코드다.

## 1. 이 파일이 있는 이유

현재 CAP 중심 경로 이전의 FastAPI 단일 애플리케이션 구현을 보존한 코드다.

이 파일은 **8. legacy — 이전 FastAPI 구조** 영역에 속한다. 독립적으로 보기보다 아래의 호출자·의존 대상·데이터 계약과 함께 읽어야 한다.

## 2. 파일 개요

| 항목 | 값 |
|---|---|
| 경로 | `legacy/fastapi/app/pipeline/review.py` |
| 형식 | `python` |
| 전체 줄 수 | 419 |
| 주석 줄 수 | 0 |
| 주요 심볼 수 | 16 |
| 환경 변수 참조 수 | 0 |

## 3. 의존성

- `__future__`
- `json`
- `re`
- `collections.abc`
- `typing`
- `app.models`
- `app.pipeline.summarize`

## 4. 주요 선언과 책임

| 선언 | 읽을 때 확인할 점 |
|---|---|
| `review_transcript` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `build_review_messages` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `note_to_review_text` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `transcript_to_indexed_text` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_select_review_segments` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_segment_score` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_replace_fold` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_heuristic_review_items` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_best_segment_index` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_dedupe_review_items` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_is_useful_review_item` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_text_in_note` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_is_known_clear_term` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_looks_like_sentence_edit` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_normalize` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |
| `_fmt_ts` | 입력, 상태 변경, 반환값과 실패 경로를 확인한다. |

## 7. 코드 흐름 상세

### 7.1 `review_transcript`

- 위치: 61~92행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.2 `build_review_messages`

- 위치: 93~140행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.3 `note_to_review_text`

- 위치: 141~154행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.4 `transcript_to_indexed_text`

- 위치: 155~165행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.5 `_select_review_segments`

- 위치: 166~191행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.6 `_segment_score`

- 위치: 192~212행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.7 `_replace_fold`

- 위치: 213~262행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.8 `_heuristic_review_items`

- 위치: 263~289행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.9 `_best_segment_index`

- 위치: 290~307행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.10 `_dedupe_review_items`

- 위치: 308~331행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.11 `_is_useful_review_item`

- 위치: 332~357행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.12 `_text_in_note`

- 위치: 358~361행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.13 `_is_known_clear_term`

- 위치: 362~394행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.14 `_looks_like_sentence_edit`

- 위치: 395~411행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.15 `_normalize`

- 위치: 412~415행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

### 7.16 `_fmt_ts`

- 위치: 416~419행
- 의미: 이 블록의 전제 조건, 외부 호출, DB/메모리 상태 변경, 예외 처리 순서가 핵심이다.

## 8. 변경 시 영향 범위
+
- 공개 계약, 상태명, 환경 변수, URL을 바꾸면 호출 측과 테스트를 함께 수정해야 한다.
- 저장 데이터의 형식이 바뀌면 기존 HANA 행과 진행 중인 작업의 호환성을 확인한다.
- 인증·소유권 검사는 편의상 우회하면 안 된다. Approuter 인증과 CAP 역할 검사는 서로 다른 계층이다.

## 9. 관련 문서

- [[00. 전체 구조와 책임 지도]]
- [[01. 녹음부터 회의록까지 End-to-End]]
- [[02. 데이터 모델과 상태 전이]]
- [[03. 인증·권한·신뢰 경계]]
