# 9. docs와 루트 설정

## 책임

이 폴더는 저장소의 `docs와 루트 설정` 관련 소스를 파일 단위로 추적한다.

## 문서 목록

- [[.cdsrc.json 완전 분석]] — 상위 모듈의 실행 또는 설정을 보조하는 파일이다.
- [[.cfignore 완전 분석]] — 상위 모듈의 실행 또는 설정을 보조하는 파일이다.
- [[.env.example 완전 분석]] — 상위 모듈의 실행 또는 설정을 보조하는 파일이다.
- [[.gitignore 완전 분석]] — 상위 모듈의 실행 또는 설정을 보조하는 파일이다.
- [[docs › transcription-architecture-migration.md 완전 분석]] — Kyma CPU Whisper에서 CF Worker + AI Core STT로 전환한 배경과 운영 기준을 기록한다.
- [[meeting_whisper.code-workspace 완전 분석]] — 상위 모듈의 실행 또는 설정을 보조하는 파일이다.
- [[mta.yaml 완전 분석]] — Cloud Foundry에 배포되는 CAP, Worker, DB Deployer, Approuter와 서비스 바인딩을 한 번에 선언한다.
- [[package.json 완전 분석]] — 저장소 전체의 실행·빌드·배포·테스트 명령과 CAP 개발/운영 프로필을 정의한다.
- [[pytest.ini 완전 분석]] — 상위 모듈의 실행 또는 설정을 보조하는 파일이다.
- [[README.md 완전 분석]] — 해당 디렉터리의 사용법과 운영 맥락을 설명한다.
- [[requirements.txt 완전 분석]] — 상위 모듈의 실행 또는 설정을 보조하는 파일이다.
- [[server.js 완전 분석]] — CAP 서버 부트스트랩. 바이너리 업로드 라우트와 정적 UI를 Express에 연결하고 요청 본문 크기를 조정한다.
- [[xs-security.json 완전 분석]] — XSUAA scope, role template, OAuth redirect URI를 정의한다.

## 읽는 법

1. 진입점과 선언형 계약을 먼저 읽는다.
2. 실제 handler 또는 실행 함수를 읽는다.
3. 호출하는 라이브러리와 테스트로 내려간다.
4. 환경 변수는 루트 `.env.example`과 `mta.yaml`의 운영 값을 함께 비교한다.
